# 패스트캠퍼스 Orca 강의 슬라이드

이 저장소는 **패스트캠퍼스 Orca 강의의 이론 슬라이드**를 만들고 배포하는 곳이다. 다른 목적의 작업은 하지 않는다.

## 모든 세션에서 먼저 읽는다

작업을 시작하기 전에 아래를 읽는다. 각 문서가 다른 것을 정하며, **섞지 않는다.**

| 문서 | 무엇을 정하는가 |
|---|---|
| `docs/curriculum.md` | 파트·챕터·클립의 번호·제목·길이. **파일 이름의 기준** |
| `docs/course-brief.md` | 청중·톤·용어 규칙·강조점·강의를 관통하는 축. **어떻게 말할지의 기준** |
| `docs/patterns.md` | 멀티에이전트 패턴 4가지의 이름·구조·색. 패턴이 등장하는 덱이면 반드시 읽는다 |
| `artifacts/<part>/_source/<clip>.md` | 그 클립의 **슬라이드 구성안 = 소스.** 무엇을 몇 장에 담을지의 기준 |

그리고 `artifacts/*/_feedback/*.md`의 `## 3. 다음부터 이렇게 해` 섹션을 전부 읽는다. 이전 덱에서 받은 지적이 누적된 곳이다.

문서가 어긋나면 **번호·제목은 커리큘럼, 내용과 장수는 클립 소스, 말투는 기획서, 패턴은 patterns.md**를 따르고 어긋난 지점을 보고에 적는다.

## 디자인 시스템을 먼저 쓴다

**슬라이드마다 CSS를 새로 짜지 않는다.** 스킬의 타입 6종(`grid-3` · `grid-2`/`vs-grid` · `list` ·
`timeline` · `fan` · `metric-row`)을 먼저 뒤지고, 변형표를 보고, 조합해 보고,
그래도 안 될 때만 커스텀을 쓴다. 커스텀만으로 채운 덱은 일반적으로 보이고 정렬 버그가 따라온다.
자세한 판정은 스킬의 「레이아웃 게이트」에 있다.

## 그림을 아끼지 않는다

**이모지 아이콘 하나로 슬라이드를 채우지 않는다.** 개념에 구조가 있으면 구조를 그린다.
라벨이 들어가는 구조도는 **인라인 SVG/CSS**로 그리고(생성 이미지는 한글 라벨을 뭉갠다),
장면·캐릭터 일러스트만 **생성 이미지**로 만들어 `_images/<clip>/`에 둔다. 자세한 기준은 스킬의 「시각 정책」에 있다.

## 슬라이드를 만드는 단위는 클립이다

계층은 **파트 → 챕터 → 클립**이고, 슬라이드는 **클립 하나에 덱 하나**다.

**모든 클립을 만들지 않는다 — `_source/<clip>.md`가 있고 그 frontmatter의 `슬라이드`가 `N장`인 클립만 만든다.** `슬라이드: 없음`이거나 소스 파일이 아직 없으면 만들지 않는다. 사용자가 지목하지 않은 클립을 먼저 만들지 않는다.

## 구조

```
docs/
  curriculum.md                    상세 커리큘럼 (세션마다 읽는다)
  course-brief.md                  강의 기획서 (세션마다 읽는다)
  patterns.md                      멀티에이전트 패턴 4가지 (패턴이 나오면 읽는다)

artifacts/
  <part>/                          파트 하나 = 폴더 하나 (part1, part2 …)
    <ch>-<n>-<slug>.html           클립 하나 = 덱 하나. 이것만 배포된다
    _source/<clip>.md              그 클립의 슬라이드 소스 (구성안·참고 자료)
    _storyboard/<clip>.md          구성 확정 문서 — HTML보다 먼저 저장한다
    _feedback/<clip>.md            슬라이드별 피드백 입력 파일
    _images/<clip>/                이 클립에서만 쓰는 이미지

assets/                            모든 덱이 공유하는 에셋 (패스트캠퍼스 · Claude Code · Codex · Orca 로고)
index.html                         덱 목록 — 커리큘럼 순서대로
```

**폴더는 파트 단위로만 나눈다.** 최상위를 종류별(`source/`, `storyboard/` …)로 쪼개면 파트 하나를 손보는 데 트리 네 곳을 오가게 된다. 종류 구분은 파트 폴더 **안에서** `_` 폴더로 한다.

**파일 이름 규칙:** `ch{챕터번호}-{클립번호}-{영문슬러그}` — 예 Part 2 › Ch02-01 → `artifacts/part2/ch02-01-setup-prep.html`.
번호는 커리큘럼의 번호를 그대로 쓴다. 슬라이드가 있는 클립만 만들기 때문에 번호는 비면서 이어지고, 그 빈 자리가 곧 커리큘럼상의 위치다.
**덱과 부속 문서는 확장자만 빼고 이름이 같다.** 이름이 어긋나면 짝을 찾을 수 없다.

- 공개 주소는 `vercel.json` rewrite로 `/<part>/<파일명>` → `/artifacts/<part>/<파일명>.html`
- HTML 안의 에셋 경로는 **루트 절대경로만** 쓴다 (`/assets/…`, `/artifacts/<part>/_images/<clip>/…`). 상대경로는 배포 주소에서 깨진다
- `_`로 시작하는 폴더와 `docs/`는 `.vercelignore`로 배포에서 제외된다

## 클립 소스 규격

슬라이드 소스는 **클립 하나에 파일 하나**이고, 덱과 같은 이름으로 `artifacts/<part>/_source/<clip>.md`에 둔다.
**`artifacts/part1/_source/ch01-01-course-intro.md`가 기준 예제다.**

파트 단위로 여러 클립이 묶인 문서를 받으면 **먼저 클립별로 쪼개 저장한 뒤** 작업을 시작한다. 묶음 파일을 그대로 두지 않는다.

```markdown
---
파트: part1 — 왜 팀이 필요한가 (강의 개요)
챕터: Chapter 1. 오리엔테이션
클립: Ch01-01. 강의 소개와 최종 결과물
길이: 11분
슬라이드: 8장                      # 없음 이면 덱을 만들지 않는다
산출물: artifacts/part1/ch01-01-course-intro.html
상태: 확정
---

# Ch01-01. 강의 소개와 최종 결과물

제작 목표: (이 클립이 달성해야 하는 것 한 줄)

#### 1. 섹션 제목

역할: (이 섹션이 덱에서 하는 일)

말할 내용

- (실제로 말할 내용)

슬라이드: (몇 장, 어떤 그림인지)
```

- **덱의 범위는 이 파일 하나다.** 옆 클립 소스를 열어 내용을 끌어오지 않는다
- frontmatter의 `슬라이드: N장`이 **총 슬라이드 수**다. 섹션별 `슬라이드:` 표기의 합과 맞아야 한다
- `슬라이드: 없음. 말로 전달`이라 적힌 섹션은 덱에 넣지 않는다
- 인용문(`>`)으로 적힌 문장은 **화면에 그대로 올릴 문구**다. 바꿔 쓰지 않는다

## 슬라이드 제작

`.claude/skills/fastcampus-slide` 스킬이 소스 분석 → 스토리보드 → HTML → 피드백 파일까지 처리한다.
덱을 만들거나 고칠 때는 **이 스킬을 쓴다.** 디자인 수치·레이아웃 타입·색 배정 규칙이 전부 그 안에 있다.

## 미리보기

루트 절대경로와 rewrite 때문에 `file://`로 열면 에셋이 깨진다.

```bash
vercel dev    # http://localhost:3000/<part>/<clip>
```
