# 패스트캠퍼스 Orca 강의 슬라이드

이 저장소는 **패스트캠퍼스 Orca 강의의 이론 슬라이드**를 만들고 배포하는 곳이다. 다른 목적의 작업은 하지 않는다.

## 모든 세션에서 먼저 읽는다

작업을 시작하기 전에 아래를 읽는다. 각 문서가 다른 것을 정하며, **섞지 않는다.**

| 문서 | 무엇을 정하는가 |
|---|---|
| `docs/curriculum.md` | 파트·챕터·클립의 번호·제목·길이. **파일 이름의 기준** |
| `docs/course-brief.md` | 청중·톤·용어 규칙·강조점·강의를 관통하는 축. **어떻게 말할지의 기준** |
| `docs/confirmed-copy.md` | **강사가 확정한 문구.** 용어·정의·프로젝트 명칭이 여기서 온다. 다른 문서와 어긋나면 이것이 이긴다 |
| `docs/patterns.md` | 멀티에이전트 패턴 4가지의 이름·구조·색. 패턴이 등장하는 덱이면 반드시 읽는다 |
| `artifacts/<part>/_source/<clip>.md` | 그 클립의 **슬라이드 구성안 = 소스.** 무엇을 몇 장에 담을지의 기준 |

그리고 `artifacts/*/_feedback/*.md`의 `## 3. 다음부터 이렇게 해` 섹션을 전부 읽는다. 이전 덱에서 받은 지적이 누적된 곳이다.

문서가 어긋나면 **번호·제목은 커리큘럼, 내용과 장수는 클립 소스, 말투는 기획서, 패턴은 patterns.md**를 따르고 어긋난 지점을 보고에 적는다.

## 표지에 목차 금지

**표지에는 목차를 절대 넣지 않는다.** `WHAT YOU LEARN`, 배우는 것, 학습 목표 목록, 핵심 주장 3개, 01·02·03 미리보기처럼 이름만 바꾼 목차도 금지한다. 표지는 클립 제목·최소한의 식별 정보·한 줄 주제·시각 1점으로 구성한다. 이 사용자 지시는 과거 피드백의 표지 목록 규칙보다 우선한다.

## 슬라이드와 발표 대본을 한 세트로 제공

앞으로 제작하는 덱은 최종 화면과 일치하는 전체 대본을 같은 루트의 `<part>/_script/<clip>.md`에 함께 저장한다. 내용·제목·장수가 바뀌면 대본도 갱신한다. 형식은 `## {클립 제목} 슬라이드 대본 (N장)`과 `## 슬라이드 i/N — {실제 제목}`이며, 말투는 사용자가 준 Hermes Bot Mode 대본처럼 자연스러운 강의 구어체다. 대본을 쓰거나 고칠 때는 **`fastcampus-script` 스킬**을 쓴다 — 흐름 패턴·말투·예제 대본·템플릿이 전부 그 안에 있다. 덱은 `fastcampus-slide`, 대본은 `fastcampus-script`로 나뉜다.

## 디자인 시스템을 먼저 쓴다

**슬라이드마다 CSS를 새로 짜지 않는다.** `.claude/skills/fastcampus-slide/assets/base-template.html`의 확정 디자인을 반영한 중립 템플릿 9종과 `references/slide-layouts.md`의 선택표를 먼저 본다. 장면·관계·열린 3열·구조도·전환 비교 중 의미에 맞는 패턴을 선택하며 카드 격자에 억지로 맞추지 않는다. 같은 역할의 타이포는 사용자가 지정한 기준 장을 따른다. 최신 기준은 터미널 문제 골든셋이다.

스킬 원본은 `.claude/skills/` 아래에 있고(`fastcampus-slide`=덱, `fastcampus-script`=대본), `.agents/skills/`의 같은 이름들은 그 원본에 연결된 심볼릭 링크다.

## 그림을 아끼지 않는다

**이모지 아이콘 하나로 슬라이드를 채우지 않는다.** 개념에 구조가 있으면 구조를 그린다.
라벨이 들어가는 구조도는 **인라인 SVG/CSS**로 그리고(생성 이미지는 한글 라벨을 뭉갠다),
장면·캐릭터 일러스트만 **생성 이미지**로 만들어 `_images/`에 둔다. 자세한 기준은 스킬의 「시각 정책」에 있다.

## 슬라이드를 만드는 단위는 클립이다

계층은 **파트 → 챕터 → 클립**이고, 슬라이드는 **클립 하나에 덱 하나**다.

**모든 클립을 만들지 않는다 — `_source/<clip>.md`가 있고 그 frontmatter의 `슬라이드`가 `N장`인 클립만 만든다.** `슬라이드: 없음`이거나 소스 파일이 아직 없으면 만들지 않는다. 사용자가 지목하지 않은 클립을 먼저 만들지 않는다.

## 구조

```
docs/
  curriculum.md                    상세 커리큘럼 (세션마다 읽는다)
  course-brief.md                  강의 기획서 (세션마다 읽는다)
  patterns.md                      멀티에이전트 패턴 4가지 (패턴이 나오면 읽는다)

artifacts/
  <part>/                          파트 하나 = 폴더 하나 (part1, part2 …)
    outputs/<ch>-<n>-<slug>.html           클립 하나 = 덱 하나. 이것만 배포된다
    _source/<clip>.md              그 클립의 슬라이드 소스 (구성안·참고 자료)
    _storyboard/<clip>.md          구성 확정 문서 — HTML보다 먼저 저장한다
    _feedback/<clip>.md            슬라이드별 피드백 입력 파일
    _images/                       이 파트의 이미지 (하위 폴더 없이 파일만)

assets/                            모든 덱이 공유하는 에셋 (패스트캠퍼스 · Claude Code · Codex · Orca 로고)
index.html                         덱 목록 — 커리큘럼 순서대로
```

**폴더는 파트 단위로만 나눈다.** 최상위를 종류별(`source/`, `storyboard/` …)로 쪼개면 파트 하나를 손보는 데 트리 네 곳을 오가게 된다. 종류 구분은 파트 폴더 **안에서** `_` 폴더로 한다.

**파일 이름 규칙:** `ch{챕터번호}-{클립번호}-{영문슬러그}` — 예 Part 2 › Ch02-01 → `artifacts/part2/outputs/ch02-01-setup-prep.html`.
번호는 커리큘럼의 번호를 그대로 쓴다. 슬라이드가 있는 클립만 만들기 때문에 번호는 비면서 이어지고, 그 빈 자리가 곧 커리큘럼상의 위치다.
**덱과 부속 문서는 확장자만 빼고 이름이 같다.** 이름이 어긋나면 짝을 찾을 수 없다.

- 공개 주소는 `vercel.json` rewrite로 `/<part>/outputs/<파일명>` → `/artifacts/<part>/outputs/<파일명>.html`
- HTML 안의 에셋 경로는 **루트 절대경로만** 쓴다 (`/assets/…`, `/artifacts/<part>/_images/…`). 상대경로는 배포 주소에서 깨진다
- 작업 문서(`_source`, `_storyboard`, `_feedback` 등)와 `docs/`는 `.vercelignore`의 명시된 규칙으로 배포에서 제외한다. **런타임 이미지가 있는 `_images/`는 배포에 포함한다**

## 클립 소스 규격

슬라이드 소스는 **클립 하나에 파일 하나**이고, 덱과 같은 이름으로 `artifacts/<part>/_source/<clip>.md`에 둔다.
**`artifacts/part1/_source/ch01-01-course-intro.md`가 기준 예제다.**

파트 단위로 여러 클립이 묶인 문서를 받으면 **먼저 클립별로 쪼개 저장한 뒤** 작업을 시작한다. 묶음 파일을 그대로 두지 않는다.

```markdown
---
파트: part1 — 왜 팀이 필요한가 (강의 개요)
챕터: Chapter 1. 오리엔테이션
클립: Ch01-01. 강의 소개와 최종 결과물
길이: 11분
슬라이드: 8장                      # 없음 이면 덱을 만들지 않는다
산출물: artifacts/part1/outputs/ch01-01-course-intro.html
상태: 확정
---

# Ch01-01. 강의 소개와 최종 결과물

제작 목표: (이 클립이 달성해야 하는 것 한 줄)

#### 1. 섹션 제목

역할: (이 섹션이 덱에서 하는 일)

말할 내용

- (실제로 말할 내용)

슬라이드: (몇 장, 어떤 그림인지)
```

- **덱의 범위는 이 파일 하나다.** 옆 클립 소스를 열어 내용을 끌어오지 않는다
- frontmatter의 `슬라이드: N장`이 **총 슬라이드 수**다. **표지 1장 + 섹션별 `슬라이드:` 표기의 합**과 맞아야 한다 (모든 덱은 표지로 연다 — 스킬 「표지 게이트」)
- `슬라이드: 없음. 말로 전달`이라 적힌 섹션은 덱에 넣지 않는다
- 인용문(`>`)으로 적힌 문장은 **화면에 그대로 올릴 문구**다. 바꿔 쓰지 않는다

## 슬라이드 제작

`.claude/skills/fastcampus-slide` 스킬이 소스 분석 → 스토리보드 → HTML → 피드백 파일까지 처리한다.
덱을 만들거나 고칠 때는 **이 스킬을 쓴다.** 디자인 수치·레이아웃 타입·색 배정 규칙이 전부 그 안에 있다.

## 미리보기

루트 절대경로와 rewrite 때문에 `file://`로 열면 에셋이 깨진다.

```bash
vercel dev    # http://localhost:3000/<part>/outputs/<clip>
```

## 슬라이드 HTML 저장 위치 — 사용자 확정

모든 슬라이드 HTML은 `artifacts/partN/outputs/<clip>.html`에 저장한다. 파트 폴더 바로 아래에 HTML을 만들지 않는다. 대본·소스·스토리보드·피드백·이미지는 각 파트의 기존 `_script`, `_source`, `_storyboard`, `_feedback`, `_images`에 둔다. 선택된 비교 덱은 정식 경로로 승격했으며 `codex`·`codex-v2`·`codex-v3` 폴더는 사용자 요청으로 삭제했다. 정식 슬라이드와 대본은 `artifacts/`의 실제 파일을 직접 수정한다.

## 확정 덱 정리 (사용자 지정)

**승격 시 작업 파일에 심볼릭 링크를 만들지 않는다.** 선택된 파일과 필요한 에셋을 정식 경로에 실제로 복사하거나 이동하고 참조 경로를 갱신한다. 임시·비교 작업 폴더는 언제든 삭제 가능해야 하며, 그 폴더에서 정식 파일을 가리키는 링크도 금지한다. 삭제 전후 정식 파일이 보존되는지 검증한다. 스킬 원본 공유용 `.agents/skills/` 링크들은 이 작업 파일 규칙과 별개다.

확정 목록은 `artifacts/CONFIRMED.md`와 `artifacts/_maintenance/confirmed-decks.json`을 따른다. 확정 목록에 있는 덱의 비교본을 다시 만들지 않는다. 슬라이드 요소 이미지·생성 후보는 각 `artifacts/partN/_images/`에 파일로 바로 보존한다. 검수용 슬라이드 캡처·브라우저 스크린샷·컨택트시트는 `artifacts/partN/_qa/`에 따로 둔다. `_images/`에 검수 캡처를 섞지 않는다. 클립별 하위 폴더나 파트 밖 통합 이미지 폴더를 만들지 않는다. HTML은 해당 루트 절대경로를 참조한다. 이전 이미지 경로와 현재 파일 대응은 `artifacts/_maintenance/image-manifest.json`에서 확인한다. Git 3개 덱은 `artifacts/part3/outputs/`로 승격했다. 비교 폴더와 버전별 인덱스는 정리했으며 승격 기록의 이전 경로는 이력으로만 남긴다.


## 실제 강의 화면 기준

기본 검수 뷰포트는 **2133×1200 CSS px**이다. 전 슬라이드를 이 크기에서 검수하고 1280×800에서 넘침을 보조 확인한다. 카드·요소 묶음은 화면을 채우도록 가로로 늘리지 말고, 글자·로고의 실제 크기에 적절한 padding과 gap을 더해 내용 기반으로 폭을 잡는다. 넓은 화면에서도 긴 빈 상자가 생기지 않게 한다.
